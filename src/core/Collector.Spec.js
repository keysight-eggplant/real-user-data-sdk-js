'use strict';
import Collector from "./Collector";

// const expect = require('chai').expect;

describe('Collector', () => {

    let Collector;
    before(() => {

    });

    describe('-> collect', () => {
        it('bla', async () => {

            let x = new Collector();
        });
    });
});